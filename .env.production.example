# ──────────────────────────────────────────────────────────────
# Hour Tracker — Production Environment Variables
# ──────────────────────────────────────────────────────────────
#
# Copy this file to .env.production and fill in real values.
# NEVER commit .env.production to version control.
#
# ──────────────────────────────────────────────────────────────

# ── Deployment (used by deploy.sh) ───────────────────────────

# Google Cloud project ID (required)
GCP_PROJECT_ID=your-gcp-project-id

# GCP region for Cloud Run deployment (default: us-central1)
GCP_REGION=us-central1

# Database VM internal IP — get from: terraform output database_internal_ip
DB_HOST=10.0.0.x

# PostgreSQL password — must match the value in terraform.tfvars
DB_PASSWORD=CHANGE_ME

# Docker image tag (default: latest)
IMAGE_TAG=latest

# ── Application (set in Cloud Run via Terraform) ────────────

# PostgreSQL connection string
# Format: postgres://USER:PASSWORD@HOST:PORT/DATABASE
# Assembled automatically by Terraform; listed here for reference.
# DATABASE_URL=postgres://hourtracker_user:PASSWORD@DB_HOST:5432/hourtracker

# NextAuth.js v5 encryption secret
# Generate with: openssl rand -base64 32
AUTH_SECRET=replace-with-a-random-secret

# Public-facing URL of the application (set automatically by Cloud Run)
# AUTH_URL=https://hour-tracker-web-xxxxxx.a.run.app

# Redis connection string
# REDIS_URL=redis://DB_HOST:6379

# ── Email (optional) ────────────────────────────────────────

# SendGrid API key for transactional emails (weekly reports, invites)
# Leave empty to disable email features.
SENDGRID_API_KEY=

# ── Scheduler ───────────────────────────────────────────────

# Cloud Scheduler authenticates to /api/reports/weekly using X-Scheduler-Key.
# This is set to AUTH_SECRET by default in Terraform.
# SCHEDULER_KEY is the same as AUTH_SECRET unless overridden.
